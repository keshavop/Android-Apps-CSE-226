package com.example.listview

class CartDataModelClass (val title:String, val description:String, val img:Int, var checked:Boolean  ) {
    fun isChecked(param: Any?, s: String, lengthLong: Int): Boolean {
        return  checked
    }
}